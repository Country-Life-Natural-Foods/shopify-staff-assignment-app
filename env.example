# Shopify Staff Assignment App Configuration

# Your app's API key and secret from Partner Dashboard
SHOPIFY_API_KEY=your_api_key_here
SHOPIFY_API_SECRET=your_api_secret_here

# Your app's URL (use ngrok for local development)
SHOPIFY_APP_URL=https://your-app-url.ngrok.io

# Session secret (generate a random string)
SESSION_SECRET=your_session_secret_here

# Required for Vercel/production (Upstash or Vercel Redis). Without this, OAuth sessions
# do not survive across serverless instances and the embedded admin reloads forever.
REDIS_URL=redis://default:password@host:6379

# Environment
NODE_ENV=development

# Optional: Enable debug logging
DEBUG=false